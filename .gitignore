# Per-machine contact API overrides — holds the real SMTP password, so it must
# never be committed. This file is deployed via git/FTP; config.local.php is not.
api/config.local.php

# Runtime cache and secrets generated by the contact API
api/storage/*
!api/storage/.htaccess
!api/storage/.gitignore
